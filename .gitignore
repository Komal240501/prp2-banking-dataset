__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
.DS_Store

# Never commit real credentials — keep only secrets.toml.example in git.
.streamlit/secrets.toml

# Keep sample data out of git if you swap in real exports with sensitive data.
# Comment the line below back in if you want to version the bundled sample CSVs.
# data/*.csv
