# Local secrets for `streamlit run app.py`.
# Streamlit reads this file automatically — do NOT commit real passwords here.
# On Streamlit Community Cloud, set the same [credentials] block under
# your app's Settings -> Secrets instead of using this file.

[credentials]
admin = "Komal123"
password = "kom@l123
